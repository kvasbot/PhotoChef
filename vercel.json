{
  "buildCommand": "flutter build web --release --web-renderer canvaskit",
  "outputDirectory": "build/web",
  "framework": null,
  "installCommand": "if ! command -v flutter &> /dev/null; then git clone https://github.com/flutter/flutter.git -b stable --depth 1 /vercel/.flutter && export PATH=\"$PATH:/vercel/.flutter/bin\" && flutter doctor; fi && flutter pub get"
}
